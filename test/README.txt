Download the latest test suite from http://www.json.org/JSON_checker/test.zip and unpack them into the same directory as this file.

If the JSON Checker is working correctly, it must accept all of the pass*.json files and reject all of the fail*.json files.
